  

    Remove GetUpcomingWeekTotalTips


    •	Time zones & grouping: If managers expect local dates, compute weekStart/end in manager local time and convert to UTC for DB queries. 
    If shifts stored in UTC use StartTimeUtc for filtering and convert to local for grouping/display.